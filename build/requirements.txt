# The renderer's freeze environment, pinned to the versions the Linux bundle shipped with.
# Python 3.12. If a pin moves, every platform's renderer moves with it in the same release.
genie_tts==2.0.2
onnxruntime==1.22.1
soxr==1.1.0
nltk==3.10.0
sherpa_onnx==1.13.4
numpy==2.5.1
pyinstaller==6.21.0
